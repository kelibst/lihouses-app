export default class House {
  constructor(id, title, description, product_img, price) {
    (this.id = id),
      (this.title = title),
      (this.description = description),
      (this.product_img = product_img),
      (this.price = price);
  }
}
